/**
 * Main pages export file
 * Organizes pages by authentication requirements
 */

// Public pages (no authentication required)
export * from './public';

// Protected pages (authentication required)
export * from './protected';
